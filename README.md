# StarryNight · AI 陪伴小手机 🌙

一个模仿 **iOS 26 原生风格**的「聊天陪伴小手机」网页版。
用手机浏览器打开就能用，电脑上打开会显示成一个手机预览框。

> 这个项目是**零构建**的：不需要安装 Node.js、不需要 npm install、不需要打包工具。
> 双击 `index.html` 就能看到效果。

---

## 一、怎么预览？（三种方式，任选一种）

### 方式 1：最简单 —— 直接双击打开
双击 `index.html` 文件，浏览器就会打开小手机。

### 方式 2：本地服务器（推荐，手机也能访问）
在项目文件夹里打开终端（PowerShell），输入：

```powershell
python -m http.server 8080
```

然后在浏览器打开：`http://127.0.0.1:8080`

想用真手机看，就让手机和电脑连同一个 WiFi，然后手机浏览器访问
`http://电脑的局域网IP:8080`（电脑 IP 可以用 `ipconfig` 查看）。

### 方式 3：VS Code 插件
装一个 **Live Server** 插件，右键 `index.html` → `Open with Live Server`。

---

## 二、文件结构（每个文件是干什么的）

```
index.html                ← 手机的整体结构（状态栏、小组件、图标、Dock 都在这里拼装）
assets/
  css/
    base.css              ← 全局颜色变量、字体、玻璃质感基础样式
    phone.css             ← 手机外壳、状态栏、灵动岛、壁纸、图标、Dock
    apps.css              ← 应用内部的样式（卡片、列表、气泡、输入框…）
  js/
    config.js             ← 【最常改】应用清单、图标、壁纸、示例角色、默认设置
    store.js              ← 全局状态 + 本地存储 + 打开/关闭应用 + 备份导出导入
    weather.js            ← 时间与天气（Open-Meteo 免费接口，失败自动降级）
    components.js         ← 界面组件（状态栏、小组件、图标、Dock、应用面板）
    views.js              ← 7 个应用页面：聊天 / 美化 / 论坛 / 世界书 / 角色集 / 用户 / 设置
    app.js                ← 启动文件：把组件注册进去并挂载
```

**加载顺序不能改**：`config → store → weather → components → views → app`。

---

## 三、当前已经做好的功能

| 区域 | 状态 |
| --- | --- |
| 顶部状态栏（时间 / 信号 / WiFi / 电量） | ✅ 时间实时走动 |
| 灵动岛、底部小白条、玻璃质感 | ✅ |
| 时间天气小组件（大号时间 + 日期星期 + 位置 + 天气） | ✅ 真实天气（按坐标自动获取） |
| 桌面应用：聊天 / 美化 / 论坛 | ✅ 可进入 |
| Dock：世界书 / 角色集 / 用户 / 设置 | ✅ 可进入 |
| 聊天 | ✅ 会话列表 + 对话界面（本地示例回复，尚未接 API） |
| 美化 | ✅ 6 套壁纸实时切换、Dock 名称开关、状态栏电量调节 |
| 论坛 | ✅ 分类筛选 + 点赞（本地示例数据） |
| 世界书 | ✅ 分类筛选 + 条目展示 |
| 角色集 | ✅ 角色卡展示，点击直达聊天 |
| 用户 | ✅ 改昵称/签名，本地自动保存 |
| 设置 | ✅ API 参数（地址/密钥/模型/temperature）、天气位置、导出/导入备份、恢复出厂 |
| 数据保存在哪里 | 浏览器 localStorage（刷新不丢，换设备不通用） |

### 还没做的（后续版本）
- 真正调用 AI 接口对话（Stage 2）
- 上传自定义壁纸、单独替换某个 APP 的图标
- 世界书 / 角色卡的新增与编辑表单
- 论坛发帖与评论（需要后端服务器）
- 用真实设备电量替换状态栏的显示值

---

## 四、想改点什么？小白常用位置

| 我想…… | 改哪里 |
| --- | --- |
| 换应用名字、加一个新应用 | `assets/js/config.js` 的 `SN.apps` |
| 换 Dock 里的应用 | `assets/js/config.js` 的 `SN.dockApps` |
| 换图标图形 | `assets/js/config.js` 的 `SN.icons` |
| 加一套新壁纸 | `assets/js/config.js` 的 `SN.wallpapers` |
| 改默认位置（比如改成北京朝阳区） | `assets/js/config.js` 的 `SN.defaults.settings`（`locationName` / `latitude` / `longitude`） |
| 改默认角色 | `assets/js/config.js` 的 `SN.defaults.characters` |
| 改整体配色 | `assets/css/base.css` 顶部的颜色变量 |
| 改手机外壳大小 | `assets/css/phone.css` 的 `.phone` |

> 小提示：改完之后如果页面没变化，按 `Ctrl + F5` 强制刷新一下（浏览器会缓存旧文件）。

---

## 五、常见问题

**Q：打开是白屏 / 一直显示“正在启动小手机”？**
A：多半是网络问题 —— Vue 是从 CDN 加载的。
按 `F12` 打开控制台看看有没有红色的报错，确认能访问 `cdn.jsdelivr.net` 后刷新。

**Q：天气一直是“24° 晴”？**
A：说明实时天气没取到（没网、或坐标不对）。到「设置 → 天气与位置」检查纬度和经度，
城市坐标可以自己搜一下填进去。

**Q：数据会丢吗？**
A：数据存在浏览器的 localStorage 里，**清空浏览器数据或换浏览器就会丢**。
重要数据请到「设置 → 导出备份」保存成 JSON 文件。

---

## 六、开发进度（Roadmap）

- [x] **Stage 1** 框架：手机外壳 / 状态栏 / 小组件 / 桌面 / Dock / 7 个页面骨架
- [ ] **Stage 2** 接入 AI：在聊天页调用你配置的 API，把世界书内容塞进提示词
- [ ] **Stage 3** 个性化：上传壁纸、自定义图标、角色卡与世界书的增删改
- [ ] **Stage 4** 论坛后端：发帖、评论、账号
